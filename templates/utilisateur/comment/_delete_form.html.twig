<form method="post" action="{{ path('app_comment_delete', {'cid': comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn button3"><i class="fa fa-trash-o" aria-hidden="true"></i></button>
</form>
