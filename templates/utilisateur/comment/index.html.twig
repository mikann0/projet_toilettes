{% extends 'base.html.twig' %}

{% block title %}Commentaire
{% endblock %}

{% block body %}
	<div class="sidebar" style="display:none;" id="sidebar">
		<button onclick="closeSidebar()" class="button">
			<iconify-icon icon="charm:cross"></iconify-icon>
		</button>
		<a href="#" class="bar-item">Voir tous vos commentaires</a>
		<a href="#" class="bar-item">Modifier votre mot de passe</a>
		<a href="#" class="bar-item">Supprimer votre compte</a>
		<a href="{{ path('app_logout') }}" class="bar-item">Se déconnecter</a>
	</div>
	<div class="filtrer-recherche">
		<a href="">
			<iconify-icon id="arrow" icon="fe:arrow-left"></iconify-icon>
		</a>
		<h1>Toilettes</h1>
		<div class="identify" onclick="openSidebar()">
			<h1>P</h1>
		</div>
	</div>
	<div class="container">
		{% for comment in comments %}
			<div class="list-commentaire">
				<div class="i-date">
					<h4>{{ comment.idToilette }}</h4>
					<p>{{ comment.dateCommentaire ? comment.dateCommentaire|date('Y-m-d H:i:s') : '' }}</p>
				</div>
				<div class="star">
					<P>{{ comment.note }}</p>
					<iconify-icon class="s-filter-star" icon="ion:star"></iconify-icon>
					<iconify-icon class="s-filter-star" icon="ion:star"></iconify-icon>
					<iconify-icon class="s-filter-star" icon="ion:star"></iconify-icon>
					<iconify-icon class="s-no-filter-star" icon="ion:star"></iconify-icon>
					<iconify-icon class="s-no-filter-star" icon="ion:star"></iconify-icon>
				</div>
				<p>{{ comment.enPanne }}</p>
				<p>{{ comment.nExistePlus }}</p>
				<p>{{ comment.accesHandicape }}</p>
				<p>{{ comment.idToilette }}</p>
			</div>
		{% else %}
			<p>no records found</p>
		{% endfor %}

	</div>

	{# <table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Note</th>
				<th>En_panne</th>
				<th>N_existe__plus</th>
				<th>Acces_handicape</th>
				<th>Commentaire</th>
				<th>Date_commentaire</th>
				<th>Id_toilette</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for comment in comments %}
				<tr>
					<td>{{ comment.id }}</td>
					<td>{{ comment.note }}</td>
					<td>{{ comment.enPanne }}</td>
					<td>{{ comment.nExistePlus }}</td>
					<td>{{ comment.accesHandicape }}</td>
					<td>{{ comment.commentaire }}</td>
					<td>{{ comment.dateCommentaire ? comment.dateCommentaire|date('Y-m-d H:i:s') : '' }}</td>
					<td>{{ comment.idToilette }}</td>
					<td>
						<a href="{{ path('app_comment_show', {'id': comment.id}) }}">show</a>
						<a href="{{ path('app_comment_edit', {'id': comment.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="9">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a href="{{ path('app_comment_new') }}">Create new</a> #}
{% endblock %}
