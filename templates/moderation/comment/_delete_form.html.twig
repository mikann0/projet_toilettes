<form method="post" action="{{ path('app_moderation_comment_delete', {'id': comment.id}) }}" 
    onsubmit="return confirm('Êtes-vous certain de vouloir supprimer ce commentaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">Supprimer</button>
</form>
