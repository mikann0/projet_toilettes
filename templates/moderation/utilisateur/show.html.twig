{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Date_ins</th>
                <td>{{ utilisateur.dateIns ? utilisateur.dateIns|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Last_login</th>
                <td>{{ utilisateur.lastLogin ? utilisateur.lastLogin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moderation_utilisateur_index') }}">back to list</a>

    <a href="{{ path('app_moderation_utilisateur_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('moderdation/utilisateur/_delete_form.html.twig') }}
{% endblock %}
